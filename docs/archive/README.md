# CashSnap Archived Docs

These notes are kept for historical context and regression archaeology. They are not active plans.

Start with root `model.md`. If an archived note matters again, copy the useful decision into `model.md` instead of creating another plan file.
